public RealEstateInformation getRealEstateInformationObject(String[] values) {
    RealEstateInformation realEstateInformation = new RealEstateInformation();

    // Set values in the respective objects
    realEstateInformation.setOwnerInformation(new OwnerInformation()
        .setOwnerName(values[0])
        .setMailingAddress(values[1]));

    realEstateInformation.setPropertyAddress(new PropertyAddress()
        .setAddress(values[2]));

    realEstateInformation.setGenralPropertyInformation(new GeneralPropertyInformation()
        .setPropertyClass(values[3])
        .setLivingUnits(values[4])
        .setZoning(values[5])
        .setNeighborhood(values[6])
        .setTaxUnit(values[13]));

    realEstateInformation.setDeedInformation(new DeedInformation()
        .setDocument1(values[7])
        .setDocument2(values[8]));

    realEstateInformation.setTractInformation(new TractInformation()
        .setNeighborhood(values[6])
        .setTract(values[9])
        .setAcres(values[11])
        .setMarketAcres(values[12]));

    realEstateInformation.setLandBasedClassificationSystem(new LanBasedClassificationSystem()
        .setFunction(values[14])
        .setActivity(values[15])
        .setOwnerShip(values[16])
        .setSite(values[17]));

    realEstateInformation.setPropertyFactors(new PropertyFactors()
        .setTopography(values[18])
        .setUtilities(values[19])
        .setAccess(values[20])
        .setFronting(values[21])
        .setLocation(values[22])
        .setParkingTypes(values[23])
        .setParkingQuantity(values[24])
        .setParkingproximity(values[25])
        .setParkingcovered(values[26])
        .setParkingUncovered(values[27]));

    realEstateInformation.setAppraisedValues(new AppraisedValues()
        .setTaxYear(values[28])
        .setPropertyClass(values[3])
        .setLand(values[29])
        .setBuilding(values[30])
        .setTotal(values[31]));

    // Print the RealEstateInformation object for debugging
    System.out.println("RealEstateInfo: " + realEstateInformation.getOwnerInformation());
    System.out.println(realEstateInformation.getPropertyAddress());
    System.out.println(realEstateInformation.getGeneralPropertyInformation());
    System.out.println(realEstateInformation.getDeedInformation());
    System.out.println(realEstateInformation.getTractInformation());
    System.out.println(realEstateInformation.getLandBasedClassificationSystem());
    System.out.println(realEstateInformation.getPropertyFactors());
    System.out.println(realEstateInformation.getAppraisedValues());

    return realEstateInformation;
}
